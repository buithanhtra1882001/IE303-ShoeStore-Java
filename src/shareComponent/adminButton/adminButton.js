import React from "react";
import { Button } from "react-bootstrap";

const Adminbutton = () => {
  return (
    <div>
      <Button variant="primary">Tải danh sách</Button>
      <Button variant="secondary">Thêm mới nhãn hàng</Button>
    </div>
  );
};

export default Adminbutton;
